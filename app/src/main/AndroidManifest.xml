<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ldsbc.reservearoom" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Bcgreenandgold"
        android:name=".dummy.App">
        <activity
            android:name="edu.ldsbc.reservearoom.RoomListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.ldsbc.reservearoom.RoomDetailActivity"
            android:label="@string/title_room_detail"
            android:parentActivityName="edu.ldsbc.reservearoom.RoomListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RoomListActivity" />
        </activity>
    </application>

</manifest>
